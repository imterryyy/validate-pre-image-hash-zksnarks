import "hashes/keccak/256bit" as keccak;

def main(private u32[8] private_data, private bool[DEPTH] directionSelector, private u32[DEPTH][8] path, u32[8] public_data, u32[8] root) {
    u8[32] h = keccak(private_data, public_data);
    assert(h == root);
    return;
}
